add = lambda a,b : a*b
print(add(398,33))

# lamba is an anonymous function that can take any number of arguments but can only have one expression. It is often used for short, simple functions that are not reused elsewhere in the code.
# In this example, we define a lambda function that multiplies two numbers and then call it with the arguments 398 and 33, printing the result.
